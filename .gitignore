# Per-service platform override generated by start.sh on each run; varies by host arch.
.docker-compose.platform.yml

# Local runtime configuration written by start.sh (secrets, project name, ports).
.env

# macOS noise
.DS_Store
